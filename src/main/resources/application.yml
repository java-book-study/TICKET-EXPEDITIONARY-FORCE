spring:
  profiles:
    active: local

  ## 이 파일 올리면 안됨!!

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
        use_sql_comments: true

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 50

  mail:
    host: smtp.gmail.com
    port: 587
    username: xe00123@gmail.com
    password: qjbsxmgegpwnyxdo
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls.enable: true

logging.level:
  root: info
  org.hibernate.SQL: debug
  org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  #org.hibernate.type: trace

app.host: http://localhost:8080

#datasource:
#  driver-class-name: org.postgresql.Driver
#  url: jdbc:postgresql://dolph.myqnapcloud.com:5432/ticket
#  username: ticketadmin
#  password: 3687
